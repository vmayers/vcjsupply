<%= form_for(@static_page_content) do |f| %>
  <% if @static_page_content.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@static_page_content.errors.count, "error") %> prohibited this static_page_content from being saved:</h2>

      <ul>
      <% @static_page_content.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
	
	<% if @render == 'contact' %>
  	<div class="field">
	    <%= f.label :contact_us %><br />
	    <%= f.text_area :contact_us, :class => "mceEditor", :size => '80x15' %>
	  </div>
	<% end %>
	
	<% if @render == 'about' %>
	 	<div class="field">
			<%= f.label :about %><br />
		  <%= f.text_area :about, :class => "mceEditor", :size => '80x15' %>
		</div>
  <div class="actions">
	<% end %>
    <%= f.submit %>
  </div>
<% end %>
